- name: install packages
  hosts: local
  connection: local
  become: yes

  tasks:
    - name: install nginx zipm packages
      ansible.builtin.package:
        name: "{{ item }}"
        state: present
      loop:
        - nginx
        - zip

       