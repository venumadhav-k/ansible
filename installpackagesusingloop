- name: install packages
  hosts: local
  connection: local
  become: yes
  task:
  - name: install packages
    ansible.builtin.package:
      name: "{{item}}"
      state: present
    loop:
     - nginx
     - zip 
       